<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentalps">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentalPS"
        android:configChanges="uiMode">

        <!-- LoginActivity sebagai launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:configChanges="uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (tanpa launcher intent-filter) -->
        <activity
            android:name="com.example.rentalps.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.RentalPS"
            android:exported="false"
            android:configChanges="uiMode"/>

        <!-- Riwayat dan Billing -->
        <activity android:name=".RiwayatBillingActivity" />
        <activity android:name=".BillingActivity" />

    </application>

</manifest>
